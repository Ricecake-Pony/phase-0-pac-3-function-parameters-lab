// Part 1
  function introduction(name) {
    return `Hi, my name is ${name}.`;
  }

//Part 2 use Two Parameters

function introductionWithLanguage(names, language) {
    return `Hi, my name is ${names} and I am learning to program in ${language}.`;
  }
  
  //Part 3 Function that uses ann Optional Parameter
  
  function introductionWithLanguageOptional(names, language = "JavaScript") {
    return `Hi, my name is ${names} and I am learning to program in ${language}.`;
  }
  
  